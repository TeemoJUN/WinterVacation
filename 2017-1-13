1:先将执行父类的静态块后子类静态块；然后再进行父类的实例区进行初始化，再父类构造器;最后子类实例区，子类构造器
2:在第一次创建对象时即，new obj（）时;先进行Class.forName();调用判断这个是否被加载进驱动，如果被加载，为对象分配内存，初始化实例区；
2.1：就好比在工厂中作业时，先看机器中是否有模板，没有就加模板进去，有就直接拿原材料进行复制，而静态区的东西就是属于模板对象的。
3：在数组中不能创建实例的持有范型的数组对象，但你可以创建非泛型的数组然后进行转型，
3.1：可以 int[][] a=new int[3][];
     不可以 int[][] a=new int[][3];
